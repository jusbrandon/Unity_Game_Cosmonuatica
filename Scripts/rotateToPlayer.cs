using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class rotateToPlayer : MonoBehaviour {
    
	// Use this for initialization
	void Start () {
        
	}
	
	// Update is called once per frame
	void Update () {
        //transform.Rotate(Quaternion.Euler(new Vector3(GameObject.Find("Konstantine").transform.position.x, GameObject.Find("Konstantine").transform.position.y, 0)));
	}
}
